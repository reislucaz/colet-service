name: CI
on: 
  push: 
    branches: 'main'

jobs:
 build:
  name: "Build"
  runs-on: ubuntu-latest
  steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-node@v4

    - run: npm run build
    